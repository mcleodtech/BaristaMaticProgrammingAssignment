/* import { TestBed } from '@angular/core/testing';

import { DrinkDispensorService } from './drink-dispensor.service';

describe('DrinkDispensorService', () => {
  let service: DrinkDispensorService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(DrinkDispensorService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
 */